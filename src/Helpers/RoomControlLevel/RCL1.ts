const RCL1Buildings = {
  run(room: Room): void {
    // console.log(room.createConstructionSite(21, 23, STRUCTURE_ROAD));
    // try console.log(room.createConstructionSite(21, 23, StructureExtension));
    // this will return any return codes by reading them out to the console
  }
};
export default RCL1Buildings;
